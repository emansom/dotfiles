{{- if gt (len .golang_no_sum_db) 0 -}}GONOSUMDB={{ .golang_no_sum_db }}{{- end }}
{{ if gt (len .golang_private) 0 -}}GOPRIVATE={{ .golang_private }}{{- end }}
{{ if gt (len .golang_proxy) 0 -}}GOPROXY={{ .golang_proxy }}{{- end }}
